#include "ExceptionFactory.h"
#include <string>
#include <iostream>
#include <sstream>

using namespace std;

namespace ccHelp
{
/*
	range_error ExceptionFactory::range2D_error(int row, int col, int maxRow, int maxCol)
	{
		stringstream stream;
		stream<<"Invalid range ("<<row<<", "<<col<<") must in ("<<maxRow<<", "<<maxCol<<")";

		return range_error(stream.str());
	}
*/
}
